import BaseViewModel from '../../common/BaseViewModel';

class ComponentsViewModel extends BaseViewModel {
    static create() {
        return new ComponentsViewModel();
    }
}

export default ComponentsViewModel;